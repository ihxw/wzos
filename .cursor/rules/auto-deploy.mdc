---
description: 功能修改完成后自动部署到默认远程主机
alwaysApply: true
---

# 修改完成后自动部署

完成涉及 **前端**、**后端** 或 **部署脚本** 的实现/修复后，在回复用户前执行部署（除非用户明确说不要部署）。

## 何时部署

- 已修改 `frontend/`、`backend/`、`deploy.ps1` 等会影响运行中的面板的代码
- 任务已构建通过（或部署脚本会自行构建）

## 何时跳过

- 仅改文档、注释、`.cursor/` 规则且不影响运行时
- 用户说「不要部署」「先别部署」等

## 命令

在仓库根目录执行（PowerShell）：

```powershell
.\deploy.ps1 -TargetHost 192.168.1.150 -User root
```

主机可在 `wzos.deploy.json` 中覆盖；部署脚本会：构建前端 → 复制到 `backend/dist` → 交叉编译 → SSH 上传并重启 `wzos` 服务。

## 完成后

在回复中简要说明：是否部署成功、访问地址 `http://<TargetHost>:8080`、若 HTTP 非 200 则说明原因。
